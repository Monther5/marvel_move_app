import 'package:flutter/material.dart';

class MoveDetails extends StatelessWidget {
  const MoveDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
    body: Center(child: Text("MoveDetails")),
    );
  }
}